import * as config from '../config';

export function filter(input: string, filterConfiguration: config.IFilterConfiguration, site:config.ISite): string {
	//TODO: フィルタ処理
	console.debug('TODO');
	return input;
}
