import * as config from '../config';
import * as logging from '../../core/logging';

const logger = logging.create('filtering');

export function filterText(input: string, filterConfiguration: config.IFilterConfiguration): string {
	//TODO: フィルタ処理
	logger.debug('TODO');
	return input;
}
